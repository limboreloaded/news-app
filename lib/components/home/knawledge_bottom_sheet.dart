import 'package:flutter/material.dart';

class KnawledgeBottomSheet extends StatelessWidget {
  const KnawledgeBottomSheet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center();
  }
}
